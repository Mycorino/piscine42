#include <stdio.h>

int main(void)
{
	char str[] = " ibc;ojvnacbjk0987t67rckgl å";
    printf("salut, comment tu vas ? 42mots quarante-deux; cinquante+et+un : %i\n", ft_str_is_printable(str));
    return(0);
}
